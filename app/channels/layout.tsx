import type { ReactNode } from "react";
import "./channels.css";
import "./channels-overrides.css";

export default function ChannelsLayout({ children }: Readonly<{ children: ReactNode }>) {
  return children;
}
